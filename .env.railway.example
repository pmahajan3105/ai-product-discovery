# =================================================================
# RAILWAY DEPLOYMENT ENVIRONMENT VARIABLES
# Copy these to your Railway service environment variables
# =================================================================

# Application Configuration
NODE_ENV=production
PORT=3001
APP_NAME=AI Product Discovery
APP_VERSION=1.0.0

# Domains (update after Vercel deployment)
FRONTEND_URL=https://aiproductdiscovery.com
CORS_ORIGIN=https://aiproductdiscovery.com,https://www.aiproductdiscovery.com

# Database (Railway PostgreSQL - update with your actual Railway database URL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway Redis - update with your actual Railway Redis URL)
REDIS_URL=${{Redis.REDIS_URL}}

# Authentication - SuperTokens
SUPERTOKENS_CONNECTION_URI=https://your-supertokens-core-url
SUPERTOKENS_API_KEY=your-supertokens-api-key

# OAuth Providers (use same as development)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# AI Services
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4-mini
EMBEDDING_MODEL=text-embedding-3-small

# Email Service
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=your-resend-api-key
SMTP_FROM_EMAIL=noreply@aiproductdiscovery.com
SMTP_FROM_NAME=AI Product Discovery

# Security (generate new secure values)
JWT_SECRET=your-super-secure-jwt-secret-min-32-chars
API_SECRET_KEY=your-api-secret-key-min-24-chars
WEBHOOK_SECRET=your-webhook-secret-min-16-chars

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_DEFAULT_MAX=1000
RATE_LIMIT_DEFAULT_INTERVAL=3600000

# Logging
LOG_LEVEL=info